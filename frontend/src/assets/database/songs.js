import { songsArray } from "../../services/api";

export {songsArray} ;

// import { songsApi } from "../../services/songsApi";

// export const songsArray = [
//     async () => {
//         try {
//             const response = await songsApi.getAll();
//             return response;
//         } catch (error) {
//             console.error('Erro ao buscar músicas:', error);
//             throw error;
//         }
//     }
// ];
